``EstimateZernikesBaseTask`` now has a ``timeout`` config field (default 600 s) that limits the multiprocessing pool during Zernike estimation. If the timeout is exceeded, an error is logged and an empty results list is returned.
