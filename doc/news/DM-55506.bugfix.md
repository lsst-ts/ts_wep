Fix ``CalcZernikesNeuralTask.empty()`` after RSO-663: ``initZkTable()`` now always includes an average row, so assigning ``ood_score = []`` raised ``ValueError: Inconsistent data column lengths`` whenever TARTS detected donuts but could not produce usable Zernikes (or found none). Empty outputs now use a length-1 NaN OOD column and mark the average row unused, matching Danish empty-table behavior.
