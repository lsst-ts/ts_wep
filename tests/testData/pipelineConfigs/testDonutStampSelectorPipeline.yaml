# This yaml file is used to define the tasks and configuration of
# a Gen 3 pipeline used for testing in ts_wep.
description: wep basic processing test pipeline
# Import calcZernikesScienceSensorSetupPipeline.yaml
imports: $TS_WEP_DIR/tests/testData/pipelineConfigs/testCalcZernikesScienceSensorSetupPipeline.yaml
# Here we specify the corresponding instrument for the data we
# will be using.
instrument: lsst.obs.lsst.LsstCam
# Then we can specify each task in our pipeline by a name
# and then specify the class name corresponding to that task
tasks:
  calcZernikesTask:
    class: lsst.ts.wep.task.calcZernikesTask.CalcZernikesTask
    config:
      donutStampSelector.maxSelect: 1
